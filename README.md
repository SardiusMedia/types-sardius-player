# types-sardius-player

Typescript types for Sardius Player.

`plugins.sardiusAds` remains direct-asset preroll/postroll. Optional
`plugins.sardiusAdServer` is the Bitmovin VAST/ad-server object (`src/adServer.ts`).

